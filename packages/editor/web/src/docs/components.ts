/**
 * 组件文档数据 — 31 个组件的 Props、Schema 示例
 * 供 PreviewView.vue 数据驱动渲染
 *
 * 此文件为兼容性重导出入口，实际数据已拆分到 ./components/ 目录下各独立文件。
 */
export { componentDocs, categoryLabels, getDocsByCategory } from './components/index'
export type { ComponentDoc, PropDoc, EventDoc, SlotDoc, ExposeDoc, SchemaExample } from './components/index'
